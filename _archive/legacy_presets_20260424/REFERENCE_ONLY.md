# Reference-Only Material

`_archive/legacy_presets_20260424/` contains retired historical preset files.
It is not part of the active ODCR pipeline.

- Do not read it from active `code/`.
- Do not execute it through `./odcr` or `python code/odcr.py`.
- Do not use it as a main configuration source, runtime fallback, or hidden config source.
- Use it only for manual history comparison or forbidden/negative-test context.
